#include <stdio.h>

int main() {
    int op1, op2, mul;      // variable declaration

    op1 = 5;                // variable definition
    op2 = 3;

    mul = op1 * op2;        // mulitiplication operation

    printf("Output of %d multiplied by %d is %d", op1, op2, mul);
}
